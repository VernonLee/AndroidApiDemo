<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vernon.lee"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="14" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".textview.TextViewActivity" />
        <activity android:name=".textview.MarqueenActivity" />
        <activity android:name=".textview.DrawableTextActivity" />
        
        <activity android:name=".communication.CommunicationActivity" />
        <activity android:name=".communication.ActivityAndBroadCastReceiverActivity" />
        
        <activity android:name=".graphics.GraphicsActivity" />
        <activity android:name=".graphics.CanvasActivity" />
        
        <receiver android:name=".communication.BroadcastReceiverOne" />
        
    </application>

</manifest>
